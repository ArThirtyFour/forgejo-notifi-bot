[bot]
token = ""
# Bot's @username (without @). Used for deep links and group invite helpers.
username = ""

[database]
models = ["app.db.functions", "aerich.models"]

[settings]
owner_id = 1218845111
throttling_rate = 0.5
drop_pending_updates = false

[api]
# Public host (or domain) if using webhooks instead of polling
host = "localhost:4454"
